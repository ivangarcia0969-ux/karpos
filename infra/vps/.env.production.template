# Karpos VPS production env — install.sh fills the __GENERATE__ values automatically.

PUBLIC_HOST=app.karpos.surcoapp.tech
KARPOS_TAG=latest

POSTGRES_DB=karpos
POSTGRES_USER=karpos
POSTGRES_PASSWORD=__GENERATE__

JWT_SECRET=__GENERATE__
JWT_EXPIRES_IN=7d
COOKIE_NAME=karpos.session
COOKIE_DOMAIN=
